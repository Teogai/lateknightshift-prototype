{
  "name": "lateknightshift-prototype",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:single": "node build-single.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:perf": "vitest run --config vitest.perf.config.js perftest/"
  },
  "devDependencies": {
    "chess.js": "^1.3.0",
    "vite": "^5.0.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "happy-dom": "^20.9.0"
  }
}
